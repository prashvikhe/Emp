id int primary key,
name varchar(20),
email varchar (20),
age int,
cv varchar(20)
)
create table Organization(
Oid int primary key,
Oname varchar(20),
email varchar (20),
)
Create table job(
JobId int primary key ,
Job type varchar(20),
Jobdecs varchar(30)
)
